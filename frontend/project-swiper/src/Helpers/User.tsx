export interface User {
  firstName: string,
  hobbies: string[],
  hours: number,
  languages: string[],
  likedProjects: string[],
  secondName: string,
  tags: string[],
  ownedProjects: string[],
  joinedProjects: string[]
}